import { defineConfig } from 'vite';
import { resolve } from 'path';

export default defineConfig({
  build: {
    lib: {
      entry: resolve(__dirname, 'src/main.ts'),
      name: 'MyBot',
      fileName: () => 'bot.js', // ★出力ファイル名を「bot.js」に固定
      formats: ['es']
    }
  }
});
